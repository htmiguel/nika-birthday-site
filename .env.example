# Neon / Vercel Postgres (Storage → Jordan-B Day → connect string)
# Vercel may expose DATABASE_URL and/or POSTGRES_URL — the app accepts either.
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Vercel Blob — created when you add the Blob store to the project
BLOB_READ_WRITE_TOKEN=

# Admin-only: visit /jordan/admin?secret=YOUR_VALUE (8+ chars). Do not share the URL.
JORDAN_ADMIN_SECRET=
